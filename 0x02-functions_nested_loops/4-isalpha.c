#include" main.h"
/**
  *_isalpha - checks for alphabetic character
  *@b: b is an ascii character
  *Return: return 1 if b
  */
int _isalpha(int b)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
	else if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

